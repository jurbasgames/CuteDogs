import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    image: {
        width: 50,
        height: 50,
        marginBottom: 8,

    },
    button:{
        display: 'flex',
        alignItems: "center",
        margin:12,
    }
})

export default styles;